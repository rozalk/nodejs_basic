const getAdd = (num1, num2) => {
  //methods for calculations....
  return num1 + num2; //returning to index.js
};
const getSub = (num1, num2) => {
  return num1 - num2; //returning to index.js
};
const getMulti = (num1, num2) => {
  return num1 * num2; //returning to index.js
};
module.exports = { getAdd, getSub, getMulti }; //exporting file...
